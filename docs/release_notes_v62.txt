HMB Sailing Log — build 62 (1.31.0+62)

Play Console limit: 500 characters per language.

=== English (en-US) ===
• Track no longer jumps: inaccurate and impossible fixes are discarded, so the distance in the log is the distance actually sailed
• Depth from the sounder is saved with log entries and printed in the PDF
• Autopilot and engine state read from NMEA: shown on the Instruments panel and logged automatically on every switch
• Engine hours counted from engine RPM
• PDF export no longer drops entries onto blank pages, and column headings repeat on every continuation page
